export const Scripts: ModdedBattleScriptsData = {
	gen: 9,
	inherit: 'gen9',
	init() {
/* 		let navlearntable: [];
		for (const mon of this.species.all()) {
			if (mon.name === mon.baseSpecies && this.modData("Learnsets", mon.id)?.learnset) {
				for (var val of this.modData("Learnsets", mon.id)) {
					if navlearntable.includes(val) {
						continue
					};
					
				}
			}
		} */
/* 		this.modData("Learnsets", "oni").learnset.risingdragon = ["9L1"];
		this.modData("Learnsets", "dragonsage").learnset.lightningelbow = ["9L1"]; */
	},
};
